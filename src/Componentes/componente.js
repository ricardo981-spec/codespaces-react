import "./estilo.css"
const lista = ["home", "cursos", "turmas"]

function Menu(){

    return(
        <ul className="header.menu">
        lista.map((item)=)(
        <li className="menu-iten">home</li>)
    </ul>
     )
}
